<h2>Komentáře</h2>

<?foreach( $comments as $comment):?>
	<div class="comment">

		<p><strong><?=$comment["userData"]["name"]?></strong><br /><em><?=$x->date($comment["userData"]["timestamp"], "d.m.Y")?></em></p>

		<p><?=$comment["content"]?></p>
	</div>
<?endforeach;?>

<?if( count($comments) == 0 ):?>
	<p>Tento článek zatím nikdo nekomentoval. Buď první!</p>
<?endif;?>